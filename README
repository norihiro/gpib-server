A server for measurement equipment connected with GPIB

Currently these interfaces are supported:
- Agilent SICL
- COM port

Recommended build environment is Scientific Linux 6/7 with mingw cross compiler.
You can build by a command `mingw32-configure && make' and get gpibserver.exe.
